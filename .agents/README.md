# Agent Notes

Use sibling repos in the parent directory for implementation references:

- `../upstage` for remodel-image workflow structure, room briefs, and generation jobs
- `../tidy` for guest-session ergonomics, workspace DTO shaping, and credit-pack flows

Prefer matching the family repo conventions before inventing new structure.
